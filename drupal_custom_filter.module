<?php

//Implements hook_views_api
//Lets Views know we're providing extensions for it to consume.

function drupal_custom_filter_views_api() {
  return array(
    "api"=>3.0, // Return the API version we're using, to make sure we're compatible with the Views version being used.
  );
}
